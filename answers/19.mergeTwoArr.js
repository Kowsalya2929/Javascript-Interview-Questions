function mergingArrays (a1,a2){
    return a1.concat(a2)
}

console.log(mergingArrays([1,2,3,4],[5,5,6,7]))

// Explanation of mergingArrays(a1, a2) function:

// The function takes two arrays (a1 and a2) as arguments.

// concat() method:

// The concat() method is used to merge two or more arrays.

// It does not modify the original arrays, it returns a new array with the values from both a1 and a2.